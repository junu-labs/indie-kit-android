<?xml version="1.0" encoding="utf-8"?>
<!--
  역할
   - indie-kit-analytics 의 매니페스트.
   - Firebase Analytics 가 요구하는 권한을 미리 선언 → lint 가 FirebaseAnalytics.getInstance 호출을 통과시킨다.
   - 사용처 앱의 매니페스트와 자동 merge 되므로 따로 적을 필요 없음.
   - 패키지 namespace 는 build.gradle.kts 의 android.namespace 가 담당.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

</manifest>
